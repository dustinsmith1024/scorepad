<%= form_for([@user,@game]) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br />
    <%= select("game", "user_id", User.all.collect {|u| [ u.email, u.id] } ) %>
  </div>
  <div class="field">
    <%= f.label :type_of %><br />
    <%= f.text_field :type_of %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
